# this image has all the pre-requisites
FROM lucwastiaux/clt-requirements:1.6
ARG CLT_CORE_VERSION
COPY utils/wait_pypi_version_update.py .
RUN python utils/wait_pypi_version_update.py --package cloudlanguagetools --version ${VERSION_NUMBER}
RUN pip3 install cloudlanguagetools==$CLT_CORE_VERSION